import { StandingsFormColorsPipe } from './standings-form-colors.pipe';

describe('StandingsFormColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new StandingsFormColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
